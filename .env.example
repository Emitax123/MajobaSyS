# ============================================================================
# MAJOBA CORE - ENVIRONMENT VARIABLES TEMPLATE
# ============================================================================
# Copiar este archivo como .env y configurar las variables según el entorno
# NUNCA commitear el archivo .env a Git
# ============================================================================

# ============================================================================
# DJANGO CORE
# ============================================================================

# Entorno: development, production, testing
DJANGO_SETTINGS_MODULE=majobacore.settings.development

# Secret Key - CRÍTICO: Generar uno nuevo para producción
# Comando: python manage.py generate_secret_key
SECRET_KEY=django-insecure-CHANGE-THIS-IN-PRODUCTION-use-generate_secret_key

# Debug - DEBE ser False en producción
DEBUG=True

# Hosts permitidos (separados por coma, sin espacios)
# Desarrollo: localhost,127.0.0.1
# Producción: tudominio.com,www.tudominio.com,tu-app.railway.app
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================================================
# BASE DE DATOS
# ============================================================================

# Desarrollo (SQLite)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Producción (PostgreSQL en Railway)
# Railway proporciona estas variables automáticamente:
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=railway
# DB_USER=postgres
# DB_PASSWORD=<proporcionado-por-railway>
# DB_HOST=<proporcionado-por-railway>
# DB_PORT=5432

# ============================================================================
# CACHE Y SESIONES (REDIS)
# ============================================================================

# Desarrollo (sin Redis)
# REDIS_URL=redis://127.0.0.1:6379/1

# Producción (Redis en Railway)
# Railway proporciona esta variable automáticamente:
# REDIS_URL=redis://<usuario>:<password>@<host>:<puerto>

# ============================================================================
# EMAIL
# ============================================================================

# Backend de email
# Desarrollo: django.core.mail.backends.console.EmailBackend
# Producción: django.core.mail.backends.smtp.EmailBackend
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Configuración SMTP (para producción)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=tu-email@gmail.com
EMAIL_HOST_PASSWORD=tu-password-de-aplicacion
DEFAULT_FROM_EMAIL=noreply@majobacore.com
SERVER_EMAIL=admin@majobacore.com
ADMIN_EMAIL=admin@majobacore.com

# ============================================================================
# SEGURIDAD
# ============================================================================

# Cookies seguras (True en producción)
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# Orígenes confiables para CSRF (separados por coma)
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# CORS - Orígenes permitidos (separados por coma)
# Solo si usas API con frontend separado
CORS_ALLOWED_ORIGINS=

# ============================================================================
# ARCHIVOS ESTÁTICOS Y MEDIA
# ============================================================================

# AWS S3 (Opcional - para archivos media en producción)
USE_S3=False
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=us-east-1

# ============================================================================
# MONITOREO Y ANALYTICS
# ============================================================================

# Sentry - Error tracking (Producción)
# Obtén tu DSN en: https://sentry.io/
SENTRY_DSN=

# Dominios adicionales permitidos para CSP
CSP_ADDITIONAL_DOMAINS=

# ============================================================================
# DJANGO ADMIN
# ============================================================================

# URL del admin (security by obscurity en producción)
# Ejemplo: secret-admin-panel/
ADMIN_URL=admin/

# ============================================================================
# INTERNACIONALIZACIÓN
# ============================================================================

LANGUAGE_CODE=es
TIME_ZONE=UTC

# ============================================================================
# RAILWAY SPECIFIC (Automáticas)
# ============================================================================

# Estas variables son proporcionadas automáticamente por Railway:
# PORT=<puerto-asignado>
# RAILWAY_ENVIRONMENT=production
# RAILWAY_STATIC_URL=https://tu-app.railway.app
# DATABASE_URL=<url-completa-postgresql>
# REDIS_URL=<url-completa-redis>

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================

# 1. NUNCA commitear el archivo .env a Git
#    - Asegúrate de que .env esté en .gitignore
#
# 2. En Railway, configurar las variables de entorno en:
#    Settings > Variables
#
# 3. Para desarrollo local, copiar este archivo:
#    cp .env.example .env
#
# 4. Generar SECRET_KEY nueva:
#    python manage.py generate_secret_key
#
# 5. Para producción, verificar configuración:
#    python manage.py check_production_settings
#
# 6. Variables críticas para producción:
#    - SECRET_KEY (nuevo, seguro)
#    - DEBUG=False
#    - ALLOWED_HOSTS (configurado)
#    - DB_* (PostgreSQL)
#    - REDIS_URL
#    - EMAIL_* (SMTP real)
#    - SESSION_COOKIE_SECURE=True
#    - CSRF_COOKIE_SECURE=True
#
# 7. Opcional pero recomendado:
#    - SENTRY_DSN (monitoreo de errores)
#    - AWS S3 (almacenamiento de media files)
